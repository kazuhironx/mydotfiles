# Copy this file to ~/.gitconfig.local and edit for your environment.
# It is loaded from the shared gitconfig via [include].

[user]
	name = Your Name
	email = you@example.com

[credential]
	helper = store
