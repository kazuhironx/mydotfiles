---
name: design-doc
description: DesignDoc.md（設計文書）の執筆・維持規範。refactoringenglish.com の effective design doc の節構成を土台に、日本語・mermaid 図・README との棲み分け・実測に基づく誠実さ・decision threshold（高コストな設計判断だけ残す）を定める。DesignDoc / 設計文書 / design doc を新規に書く・改訂・レビューするときに加え、実装・リファクタリング・機能追加・バグ修正の途中で設計判断（責務、境界、インターフェースの形、制約、代替案の採否）が変わったときにも、明示的な依頼がなくても使用する。コード変更タスクの完了前に「DesignDoc 更新が必要か」の判定チェックを必ず実行する。
---

# DesignDoc の執筆・維持規範

機能・コンポーネントの設計文書（DesignDoc.md）を書く・更新するときは、以下に従う。

## 併用する規範

作業前に `../japanese-tech-writing/SKILL.md` と `../cognitive-rhythm-writing/SKILL.md` を読む。

## いつこの skill を使うか

次のいずれかに該当したら適用する。**2 と 3 は依頼者が「DesignDoc を更新して」と言わなくても自分で判断して適用する。**

1. DesignDoc の新規執筆・改訂・レビューを依頼されたとき。
2. コード変更タスク（実装・リファクタリング・機能追加・バグ修正）の途中で、設計判断を新しく下した、または既存の判断を覆したとき。
3. コード変更タスクの完了前。対象コンポーネントに DesignDoc.md が存在するなら、後述の「実装後の更新判定」を必ず実行する。

## 置き場所と言語

- 対象コンポーネントのソースと同じディレクトリに `DesignDoc.md` として置く。
- 本文は日本語。コード・識別子・固有名は原綴りのまま。

## README.md との棲み分け

- **README.md**：日々の操作リファレンス（ビルド手順、環境変数、エンドポイント一覧など）。具体値の単一の情報源。実装が変わるたび更新される。
- **DesignDoc.md**：設計判断の記録（目的、境界、構成、制約、棄却した代替案、未解決の課題）。判断が変わったときだけ更新される。
- 具体値（ポート、env 名、パス）は README にだけ書き、DesignDoc からは参照する。重複させない。
- 両ファイルの冒頭で相互に参照し、この分担を一文で宣言する。

## 節構成

次の順で構成する。これは**メニュー**であり、対象に正直に当てはまらない節（Timeline、Privacy、Legal 等）は省いてよい。逆に、埋められない節を形だけ埋めない。

1. **タイトルとメタデータ**：冒頭に表で「著者 / 作成日 / 状態 / 正本（このファイルのパス）」。著者は依頼者に確認する（既定は git の user 名）。状態は「草案 / 実装済」のような安定した値だけにする。「PR レビュー中」のようなライフサイクル依存の値は、マージした瞬間に陳腐化して更新を強いるので書かない。
2. **目的（Objective）**：この機能が何のためにあるかを一文で。実装語彙を使わない。
3. **背景**：問題の文脈。なぜ今これが必要か、既存手段はなぜ不足か。因果には機構（なぜそうなるか）を一文添える。
4. **関連文書**：README、関連 PR/issue、前提となる変更へのリンク。
5. **ゴール**：ユーザーや製品への影響で書く。実装内容で書かない（「〜コンポーネントを追加する」ではなく「検証者が〜できる」）。
6. **非ゴール**：やらないことを明示し、それぞれに理由を一文添える。スコープ論争の予防が目的。
7. **シナリオ**：完成したシステムを使う場面を、行為者を主語にした時系列で 1〜2 個。
8. **システム構成（図）**：後述の図の規範に従う。図の直後に、構成の要点を 2〜4 個の太字見出し付き段落で述べる。
9. **用語集**：文書内で定義した術語だけを載せる。一般語は載せない。
10. **制約**：toolchain・環境・依存の制限と、それが設計に与えた影響。
11. **性能目標と監視**：測定可能な目標と、その逸脱をどう検知するか。debug ツール等では軽量でよいが「実測済みか未測定か」を区別して書く。
12. **インターフェース**：設計として決めた形（API の様式、エラーの返し方）だけを書き、一覧は README へ参照。
13. **セキュリティ**：脅威モデル（誰が攻撃者か）を先に一文で定め、境界と対策を対応づける。「対策なし」の場合はその根拠を書く。
14. **ログ**：何を、なぜ記録するか。
15. **未解決の課題（Open Issues）**：問題 + 現状 + 次の一歩。設計承認をブロックしない事項の置き場。
16. **解決済みの課題（Resolved Issues）**：設計途中で遭遇し解決した論点。原因と解決を一段落で。
17. **検討した代替案**：棄却した案それぞれに棄却理由を付ける。理由は「なぜ採用案より劣るか」の機構まで書く。

## 取捨選択の基準

- 記載する決定は「**間違えたときのやり直しコストが高いもの**」に絞る（decision threshold）。自明な選択やいつでも変えられる値は書かない。
- 新任のチームメンバーが、口頭の補足なしに読み切れることを基準にする。

## 実装後の更新判定

コード変更を終えたら、変更内容を decision threshold にかける。判定の軸は一つだけである。
**この変更は「間違えたときのやり直しコストが高い判断」を新設・変更・無効化したか。**

Yes なら該当節だけを更新する。No なら DesignDoc に触らない。
過剰更新は劣化である。DesignDoc が実装の変更ログに近づくほど、判断の記録としての価値が下がる。迷ったら更新しない。

### 更新すべき変更の例

- コンポーネント間の**責務の移動・分割・統合**（何がどこの仕事かが変わった）
- **境界の変更**：プロセス分割、通信経路、データの所有者の変更
- **インターフェースの様式の変更**：API のエラーの返し方、同期/非同期の切替（個々のエンドポイント追加ではなく「形」の変更）
- 新しい**制約**の発見・追加（toolchain の制限で設計を曲げた、依存を固定した）
- 記載済みの**代替案の採否が逆転**した（棄却した案を採用した、採用案を捨てた）
- **性能目標・脅威モデルの前提**が変わった
- 「未解決の課題」に挙がっていた論点を実装で**解決した**（Resolved Issues へ移す）
- DesignDoc の記述と実装が**矛盾する状態になった**（どちらが正か判断し、DesignDoc が誤りなら直す）

### 更新不要な変更の例

- バグ修正（設計判断を変えずに実装を意図に合わせただけ）
- 関数・ファイル単位のリファクタリング（責務の境界をまたがない整理）
- 具体値の変更：ポート、env 名、パス、タイムアウト値（README の担当）
- エンドポイント・設定項目の**既存の様式に沿った**追加（一覧は README の担当）
- 依存ライブラリのバージョン更新（設計上の制約が変わらない限り）
- ログメッセージ・エラーメッセージの文言調整
- テストの追加・修正

### 自己チェック

コード変更タスクの完了報告の前に、次を順に答える。

1. 対象コンポーネントに DesignDoc.md はあるか。→ なければ何もしない（新規作成はやり直しコストが高い判断を複数含む場合のみ提案する）。
2. この変更を 6 か月後の新任メンバーが読んだとき、「なぜこうなっているのか」と口頭で聞きに来る判断が含まれるか。→ No なら更新しない。
3. その判断は「更新すべき変更の例」のいずれかに該当するか。→ 該当しないなら、更新しない理由を一文で言えることを確認して終わる。
4. 該当するなら、影響する**節だけ**を更新する（目的・背景など無関係な節を書き直さない）。更新後、README との具体値の重複が生じていないか確認する。
5. 更新した／しなかったの判断を、完了報告に一文で添える（「責務の移動はないため DesignDoc は未更新」など）。

## 図の規範

- 図は **mermaid をコードブロックで md に直接埋め込む**（GitHub がネイティブレンダリングするため、SVG 化は不要）。
- mermaid で表現しきれない図（自由レイアウトの物理構成図など）に限り drawio で描き、SVG に export して md に埋め込む。**この場合 .drawio ソースも commit する**（編集可能性の維持）。
- 図は最小限（構成 1 枚 + 論点のある機構 1 枚程度）。図で伝わることを本文で繰り返さない。

## 誠実さ

- 実測・実機で確認した事実と、未確認の想定を区別して書く（「実測 2 秒」「長期観測していない」）。
- 検証していないことを検証済みのように書かない。性能値・挙動は測ってから書くか、未測定と明記する。

## 書き終えたら

- japanese-tech-writing の点検リスト（LLM っぽい表現、論証の厳密さ、冗長）で自己点検する。
- README との重複（具体値の二重記載）がないか確認する。
- mermaid がレンダリング可能か確認する（GitHub の preview か mermaid.live 相当の構文チェック）。
